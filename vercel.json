{
  "buildCommand": "pnpm --filter @cavatrapi/app build:web",
  "outputDirectory": "apps/app/dist",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
